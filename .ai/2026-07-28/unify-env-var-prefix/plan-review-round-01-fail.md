# Plan 评审:第 01 轮

## 问题清单
- [major] R-01 旧名残留与 K8s 校验命令的正则写法失效，无法覆盖方案声称的收口验收。
  - 详情:定位：plan.md 的 TC-03、TC-06。两条命令都在 `grep -E` 中把分支符写成了 `\|`；在 ERE 中这会匹配字面量 `|`，实际对 `AGENT_ID` 以及 `name: DOPILOT_AGENT_ID` 均无命中。TC-03 又把退出码 1 定义为成功，因此即使旧名大量残留也会假通过；TC-06 则会失败。此外，TC-03 修成真正的 alternation 后仍会命中 `apps/agent/tests/test_command_consumer.py` 等文件中作为普通 Python 常量使用的 `AGENT_ID`，与“部署面残留为 0”的预期口径不一致。请改用正确的 ERE 分支语法，并把扫描范围或匹配上下文限定到实际部署环境变量消费与文档引用；TC-06 还应以可判定脚本同时断言新键、`fieldRef` 关联及旧键缺失。

## 总评
方案的改名路径、破坏性升级策略和证据档位声明总体自洽，未发现架构层冲突。7 条用例均逐条声明 A 档与证据形态，C 档为 0；但静态收口测试必须先修正，否则核心验收会假通过且 K8s 用例无法成功。

VERDICT: fail
