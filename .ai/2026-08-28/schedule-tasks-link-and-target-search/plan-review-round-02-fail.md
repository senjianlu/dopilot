# Plan 评审:第 02 轮

## 问题清单
- [major] R-01 防抖回调可能用旧筛选快照发起最新请求，导致筛选状态与结果不一致
  - 详情:定位：plan.md:166-217、TC-10/TC-13。方案规定搜索变化时定时 300ms 后直接调用带六个参数的 `load()`，但未定义待执行回调如何取得最新的 status、build、schedule 和 pageSize。若输入搜索词后在 300ms 内修改下拉筛选或清除调度芯片，定时回调可能携带修改前的筛选值发起请求；它反而拥有更大的 `reqSeq`，因此会覆盖正确请求。TC-13 只验证搜索已经生效后的翻页、页大小和刷新，无法捕获该路径。应明确用最新状态重建请求（例如防抖仅提交 `appliedSearch`，由统一查询效应读取完整当前筛选，或通过 refs/取消重排保证最新快照），并增加“防抖等待期间修改其他筛选/清除调度”的受控测试。

## 总评
后端过滤、请求透传、静态导出验证和逐条 A 档证据契约均已覆盖，C 档为 0，符合证据约束。当前仍有一个会造成组合筛选结果错误的防抖状态竞态，因此需修订方案后重评。

VERDICT: fail
