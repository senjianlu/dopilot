# Plan 评审:第 03 轮

## 问题清单
- [major] R-01 测试未证明限批和活跃任务过滤发生在 SQL 侧，原 OOM 缺陷可在用例全部通过时仍然存在。
  - 详情:定位：plan.md:86-121、148-152、220-226。TC-01 只验证终态行未被重排，现有“无界加载后逐行跳过”实现也能通过；TC-02/TC-07 只验证处理数量，先 `.all()` 全量物化再在 Python 中切片同样能通过。因此测试没有锁定本次修复的核心验收标准——数据库查询自身包含 JOIN/活跃状态谓词及 LIMIT。应在 TC-01/02/07 中通过 SQLAlchemy execute/事件捕获或等价的可自动化 SQL 断言，明确验证 reconcile SELECT 的活跃任务过滤与 LIMIT，以及 dispatch SELECT 的 ORDER BY/LIMIT 均在 SQL 语句中生效。
- [major] R-02 TC-02/TC-09 无法检出页尾游标在 flush 后取值这一已识别的饥饿错误。
  - 详情:定位：plan.md:91-102、130-138、221、228。若错误实现等处理及 `notif.notify` flush 后才读取 `rows[-1].updated_at`，TC-02 第二次调用会先得到空页，但“空页同轮回绕”仍会取到第 3 条，因此照样通过；TC-09 的页尾 X 消息存在、不会改写 updated_at，也不覆盖游标污染。该错误在持续有新 sent 行时会不断把游标推到新时间并饿死旧行。应让 TC-02 直接断言首轮游标等于 SELECT 后保存的原始页尾元组，或在两轮间持续插入排序键更晚的新行并断言旧的第 3 条仍按正确次序被检查。

## 总评
方案的 keyset 回绕、reclaim/lost 排除和证据档位契约本身已基本自洽，所有用例均逐条声明 A 档及完整证据形态。当前失败仅因两项核心回归测试仍允许错误实现通过，需补强后再进入实现。

VERDICT: fail
