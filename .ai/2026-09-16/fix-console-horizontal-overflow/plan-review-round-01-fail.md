# Plan 评审:第 01 轮

## 问题清单
- [major] R-01 完整名称的验收只检查 title 属性，未验证 tooltip 实际可达。
  - 详情:定位：plan.md:99-101、134、153-154。目标要求截断后仍能通过 tooltip 查看完整名称，但 TC-02 仅检查 className 和 title；即使遗漏 TooltipContent 或触发器不可用，该用例仍会通过。原生 title 也不能保证无 hover 环境可读取，方案中的文本 span 未明确提供键盘聚焦入口。建议明确可聚焦的触发方式，并增加 A 档交互断言：分别通过 hover 和键盘聚焦显示名称、模板的完整原文。现有徽标 tooltip 测试不覆盖这两个新增触发器。
- [major] R-02 徽标不被截断的边界验收缺少真实布局验证。
  - 详情:定位：plan.md:103-105、138-139。TC-06 通过渲染后查询元素及可见性判断“未被截断容器吃掉”，但现有单测使用 jsdom，不计算布局，无法识别 overflow 裁剪；即使徽标仍放在截断容器内，该断言也可能通过。唯一浏览器布局用例 TC-07 未要求包含自动禁用徽标和 ArchivedIndicator，也未检查其裁剪情况。建议将“超长名称＋自动禁用徽标＋已归档模板”加入 TC-07 的明确数据条件，在真实浏览器验证徽标位于裁剪区域之外、操作入口可达，并检查 1440 视口下的表格宽度；保留 A 档完整输出。

## 总评
根因修复和菜单化方向与现有组件结构、rawf 闸门及轮次配置相容；测试表已逐条声明档位与证据形态，C 档为零。当前测试仍可能在完整名称不可达或徽标被裁剪时通过，需补齐这两处验收覆盖后重评。

VERDICT: fail
