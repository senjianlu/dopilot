# Plan 评审:第 02 轮

## 问题清单
- [major] R-01 全量基线用例遗漏了明确列为受管增量的 Scrapy skill 路径
  - 详情:定位：步骤 2、TC-01，以及背景增量③和偏离项 D-4。方案称比较 21 个受管路径，却期望 19 个 IDENTICAL 加 2 个 EXPECTED-DIFF；但 `.claude/skills/rawf-stack-scrapy/SKILL.md` 在 v1.0.0 中存在、consumer 中按 D-4 有意缺席，也必须成为第三个 EXPECTED-DIFF。按方案当前计数，脚本要么只检查了 20 个路径、漏掉 D-4，要么检查 21 个路径却无法满足预期。应明确完整路径清单，并调整为 18 个 IDENTICAL 加 3 个 EXPECTED-DIFF，同时严格断言第三项仅因 D-4 而缺席。
- [major] R-02 测试表混用了互不兼容的工作目录，按所列命令无法完整执行
  - 详情:定位：TC-01 至 TC-08 的“步骤”。TC-01、TC-04使用仓库根相对路径 `.ai/...`，而 TC-02、TC-03、TC-05、TC-06、TC-08直接调用 `evidence/...`；方案没有声明切换工作目录，且两类命令不可能在同一当前目录下同时成立。按通常从仓库根执行的方式，后六类 `evidence/...` 命令会找不到文件，无法形成承诺的 A 档证据。应为每条用例明确 cwd，并统一使用仓库根下的任务目录完整相对路径，或统一先 `cd` 到任务目录后相应改写所有命令。

## 总评
方案的证据档位契约合规：8 条均逐条声明为 A 档并给出原始输出形态，C 档为 0，异常路径也有覆盖。但全量受管路径计数存在实质漏项，且测试命令的工作目录不自洽，按当前方案实施会在留证阶段返工，因此判定 fail。

VERDICT: fail
