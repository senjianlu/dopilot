---
status: draft
task: <task-slug>
date: <yyyy-mm-dd>
approved_at: 
---

# 方案:<标题>

## 背景与目标

## 改动范围
<!-- 涉及的文件/模块,以及明确不动的部分 -->

## 实现方案

## 测试用例
<!-- 必填。每条:编号 TC-NN、档位、前置条件、步骤、预期结果、证据形态。
     至少一条非 happy-path。
     档位(权威定义见 .ai-workflow/review-standards.md「测试证据档位」):
       A = 可由非交互命令判定(测试/lint/tsc/grep/脚本)。凡可自动化者
           一律 A 档,不得降档;证据 = 命令 + 完整原始输出 + 退出码。
       B = 靠静态事实即可确证;证据 = 日志片段,或代码/文档引用,
           **B 档引用须带 `文件:行号`**,供评审 grep 复核。
       C = 确实无法自动化的人工交互(真机视觉、剪贴板、系统弹窗);
           证据 = 实现者陈述 + 关键锚点,且 **C 档须写明不可自动化的理由**。
       C 档条目 ≤ 用例总数 1/3(总数 < 3 时至多 1 条);截图不得作为
       任一用例的唯一证据(评审在只读沙箱中无法读图)。 -->
| 编号 | 档位 | 前置条件 | 步骤 | 预期结果 | 证据形态 |
|---|---|---|---|---|---|

## 风险与回滚

<!-- 过程产物落点:体量较大的验证证据(日志、截图)放任务目录 evidence/,
     引用素材(Design 稿、参考图)放 assets/;二者须在评审前落盘。 -->
