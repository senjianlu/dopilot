# dopilot phase-1 demo Scrapy project config.
# Project name: demo  |  Spider name: phase1
# Used by automated tests and the compose smoke; must NOT import reference/scrapydweb.

[settings]
default = demo.settings

[deploy]
project = demo
